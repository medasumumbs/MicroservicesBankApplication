apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.36.0 (ae2a39403)
  labels:
    io.kompose.service: exchange-generation-service
  name: exchange-generation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: exchange-generation-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -c
        kompose.version: 1.36.0 (ae2a39403)
      labels:
        io.kompose.service: exchange-generation-service
    spec:
      containers:
        - env:
            - name: NOTIFICATIONS_SERVICE_PORT
              value: "8081"
          envFrom:
            - configMapRef:
                name: env
          image: exchange-generation-service
          name: exchange-generation-service
          ports:
            - containerPort: 8092
              protocol: TCP
      restartPolicy: Always
