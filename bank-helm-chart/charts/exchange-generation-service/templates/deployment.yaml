apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.36.0 (ae2a39403)
  labels:
    io.kompose.service: exchange-generation-service
  name: exchange-generation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: exchange-generation-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -c
        kompose.version: 1.36.0 (ae2a39403)
      labels:
        io.kompose.service: exchange-generation-service
    spec:
      containers:
        - env:
            - name: NOTIFICATIONS_SERVICE_PORT
              value: "8081"
            - name: SPRING_CONFIG_LOCATION
              value: file:///app/config
          volumeMounts:
            - mountPath: /app/config
              name: config-volume
          image: exchange-generation-service
          name: exchange-generation-service
          ports:
            - containerPort: 8092
              protocol: TCP
      volumes:
        - name: config-volume
          configMap:
            name: {{ include "exchange-generation-service-appliaction.fullname" }}
      restartPolicy: Always
